Task 4: SPM experiments

Contains:

- `spm_model.py` — simple SPM scaffold (ohmic + OCV mapping).
- `train_spm.py` — small fitter that estimates an ohmic resistance and runs a demo simulation.

Example:

```
python task4/train_spm.py --phys_csv data_out/battery_phys_resampled200.csv
```
